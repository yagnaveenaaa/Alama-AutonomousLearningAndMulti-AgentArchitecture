[project]
name = "auth-bug-demo"
version = "0.1.0"
requires-python = ">=3.12"
description = "Fixture Python repo with an authentication bug for Alama vertical slice."

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"
